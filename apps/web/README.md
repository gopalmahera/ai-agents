# DAI — Web UI

Next.js 14 admin console for **Dozee Alert Intelligence (DAI)**. All data is fetched from the NestJS API at `NEXT_PUBLIC_API_URL`.

## Local dev

```bash
cp .env.local.example .env.local
npm install && npm run dev
```

Open http://localhost:3000
