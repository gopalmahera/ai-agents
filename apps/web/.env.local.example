# Leave empty to proxy API through Next.js rewrites (recommended in Docker).
# Set to http://localhost:4000 for direct browser → API calls.
NEXT_PUBLIC_API_URL=
API_INTERNAL_URL=http://localhost:4000
NEXT_PUBLIC_ADMIN_TOKEN=
