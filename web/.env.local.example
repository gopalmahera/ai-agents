# Proxy /api/* to the Flask backend (used by next.config.mjs rewrites).
API_URL=http://localhost:8080

# Leave empty to use the /api proxy above (recommended for docker compose).
# Set to call Flask directly from the browser (e.g. http://localhost:8080).
NEXT_PUBLIC_API_URL=

NEXT_PUBLIC_ADMIN_TOKEN=
