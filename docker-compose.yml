services:
  alert-agent:
    build:
      context: .
      platforms:
        - linux/amd64
    platform: linux/amd64
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./alert-agent/routing.yaml:/app/alert-agent/routing.yaml:ro
    environment:
      PROMETHEUS_URL: ${PROMETHEUS_URL}
      LOKI_URL: ${LOKI_URL}
      LOGS_DIR: /app/logs
      ROUTING_CONFIG_PATH: /app/alert-agent/routing.yaml
    extra_hosts:
      - "host.docker.internal:host-gateway"
